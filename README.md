### How to create a virtual environment in Python:

```bash
> python3 -m venv <virtualenv-name>
```

# Future Refactoring

### Key points:

- Add a `requirements.txt` file.
- Create a `.env` file to put the environment variables in there.
- Create a `.gitignore` file to avoid the  submission of the `.env` file into the repository.
- Learn about how to use `pyenv` and `pyenv virtualenv` to manage virtual environments.
- Learn about how to use `poetry` to manage dependencies.